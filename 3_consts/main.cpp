#include <iostream>

int main() {
    /*
     * Variables declared with const cannot be changed and must be defined at declaration.
     */
    const double pi  = 3.14;
    std::cout << pi;

    return 0;
}